import { StyleSheet } from 'react-native'

export default Styles = StyleSheet.create({
    view: {
        flex: 1,
        height: '100%'
    },
    fullView: {
        flex: 1,
        height: '100%',
    },
    imageStyle: {
        alignItems: 'center',
        justifyContent: 'center'
    }
})